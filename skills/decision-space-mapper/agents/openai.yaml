interface:
  display_name: "Decision Space Mapper"
  short_description: "Map the full option space before choosing"
  default_prompt: "Use $decision-space-mapper to extract my current options, expand the missing alternatives, and map the full decision space before recommending a path."
