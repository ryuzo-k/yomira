interface:
  display_name: "Decision Space Mapper"
  short_description: "Generate concrete candidates before choosing"
  default_prompt: "Use $decision-space-mapper to generate concrete candidate outputs for this decision before recommending a path."
